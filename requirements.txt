# Relformer runtime dependencies.
#
# detectron2 is NOT pip-installable from PyPI for arbitrary torch/cuda combos.
# Install it manually after torch, e.g.:
#   pip install 'git+https://github.com/facebookresearch/detectron2.git@v0.6'
# CenterNet2 is vendored under third_party/ and added to sys.path at runtime.

# Core ML stack — versions pinned to match the original GRiT release.
transformers==4.21.1
deepspeed==0.7.0
timm==0.6.7
einops==0.4.1
Pillow==9.0.0
pydantic==1.9.1
protobuf==3.19.6
scikit-image==0.19.0
opencv-python==4.5.5.64

# Detection / captioning utilities
lvis
nltk
ftfy
regex
inflect
fasttext

# General
boto3
mss
matplotlib
requests
scikit-learn
pyyaml
anytree
tqdm
dataclasses
